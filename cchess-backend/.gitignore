node_modules/
dist/
*.log
.env
.env.local
serviceAccount*.json
# Pikafish binary/NNUE download dir (cchess-backend/engine/). Anchored with a
# leading slash so it does NOT also ignore TypeScript source under src/engine/.
/engine/
lab/reports/
